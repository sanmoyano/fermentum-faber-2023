import { Stack, Text } from "@chakra-ui/react";

const BarList = ({ bares, name }) => {
    return (
        <Stack>
            <Text>barlist</Text>
        </Stack>
    );
};

export default BarList;
