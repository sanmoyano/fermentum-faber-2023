import { Stack, Textarea } from "@chakra-ui/react";

const Form = () => {
    return (
        <Stack bgColor={"green.500"} width={"100%"}>
            <Textarea placeholder="Here is a sample placeholder" />
        </Stack>
    );
};

export default Form;
